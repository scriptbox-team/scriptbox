import DisplaySystem from "core/systems/display-system";

describe("DisplaySystem", () => {
    test("temp test", () => {
        // The display system is going to undergo some changes
        // Since entity sprites are not yet scripting-controlled
        // So I'm going to defer writing tests for this to after that happens
    });
});
