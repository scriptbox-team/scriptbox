import ServerMessage from "networking/server-messages/server-message";

/**
 * A system which is used by the Server class to perform various behaviours.
 * Ultimately, the systems ended up being different enough that there wasn't any shared
 * behaviour between them, so this is mostly a holdover from the early design.
 * @module core
 */
export default abstract class System {
}
